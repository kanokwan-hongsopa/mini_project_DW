version: 2

models:

  - name: stg_bookings
    description: "Cleaned booking records"
    columns:

      - name: book_ref
        description: "Unique booking reference"
        data_tests:
          - not_null
          - unique

      - name: book_date
        description: "Date and time of booking"
        data_tests:
          - not_null

      - name: total_amount
        description: "Total amount of the booking"
        data_tests:
          - not_null


  - name: stg_tickets
    description: "Cleaned passenger ticket records"
    columns:

      - name: ticket_no
        description: "Unique ticket number"
        data_tests:
          - not_null
          - unique

      - name: book_ref
        description: "Booking reference"
        data_tests:
          - not_null
          - relationships:
              arguments:
                to: ref('stg_bookings')
                field: book_ref

      - name: passenger_id
        description: "Passenger identifier"
        data_tests:
          - not_null


  - name: stg_ticket_flights
    description: "Cleaned ticket-flight segment transactions"
    columns:

      - name: ticket_no
        description: "Ticket number"
        data_tests:
          - not_null
          - relationships:
              arguments:
                to: ref('stg_tickets')
                field: ticket_no

      - name: flight_id
        description: "Flight identifier"
        data_tests:
          - not_null

      - name: fare_conditions
        description: "Fare class such as Economy, Comfort or Business"
        data_tests:
          - not_null

      - name: amount
        description: "Ticket amount for the flight segment"
        data_tests:
          - not_null